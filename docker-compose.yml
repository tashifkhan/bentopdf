services:
  bentopdf:
    build:
      context: .
      dockerfile: Dockerfile
    # Or pull a published image:
    # image: ghcr.io/tashifkhan/bentopdf-ui:edge
    container_name: bentopdf
    restart: unless-stopped
    ports:
      - '3000:3000'
